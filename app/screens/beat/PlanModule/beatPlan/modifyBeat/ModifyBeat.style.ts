import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {marginHorizontal: 20, marginTop: 24, flex: 1},
  button: {
    marginBottom: 20,
  },
  flatlist: {marginBottom: 10},
});
export default styles;
